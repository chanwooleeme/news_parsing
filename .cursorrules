# Global rules (applies to all modules)
ROLE: Senior Python Developer  
LANG: Python 3.10+  
STYLE:  
- Follow Google Python Style Guide  
- Enforce type hints (PEP 484)  
- Use Black for auto-formatting  

# Module-specific rules  
@/airflow:  
  FRAMEWORK: Apache Airflow 2.7+  
  DAG_RULES:  
    - Use >> for task dependencies  
    - Prefer TaskFlow API over traditional Operators  

@/html_parser:  
  LIBRARY: BeautifulSoup4  
  PARSING_RULES:  
    - Use CSS selectors first  
    - Always add error handling